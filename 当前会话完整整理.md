# 当前会话完整整理

## 0. 会话目标与背景

- 项目主题：基于 ART + MMDetection3D 的 3D 对抗鲁棒性研究。
- 研究方向演进：从纯点云逐步收敛为多模态主线（LiDAR + Camera）。
- 主要任务类型：环境搭建、项目结构整理、脚本开发、文档撰写、GitHub 发布、迁移与排障。

---

## 1. 关键对话时间线（按会话顺序整理）

### 1.1 环境与项目初始化阶段

1. 你提出：希望帮助配置 ART 和 MMDetection3D 项目环境。
2. 处理结果：完成 Ubuntu 容器环境安装流程并验证关键依赖组合。
3. 已确认的核心依赖组合（会话中反复提及并验证）：
   - torch 2.11.0+cu130
   - mmcv 2.1.0
   - mmengine 0.10.7
   - mmdet 3.2.0
   - mmdet3d 1.4.0
   - ART 1.20.1
   - open3d 0.19.0

### 1.2 文档与结构整理阶段

1. 你提出：需要安装文档、测试工作流、方向建议与可交付文档。
2. 处理结果：完成 docs 目录下多份文档编写与结构整理。
3. 你提出：需要 requirements.txt 与 environment.yml。
4. 处理结果：已生成并纳入项目。
5. 你提出：需要推荐目录结构并落地。
6. 处理结果：完成 configs / data / outputs / scripts / docs / work_dirs 结构化。

### 1.3 配置模板与统一流程脚本阶段

1. 你提出：希望有可运行模板。
2. 处理结果：完成 base、pointcloud、multimodal 配置模板。
3. 你提出：希望统一执行入口。
4. 处理结果：新增 run_pointcloud_pipeline.py，支持 clean / adversarial / defended / all。

### 1.4 可视化阶段

1. 你问：npy 文件怎么打开。
2. 我给出：加载和可视化方法。
3. 你确认：需要可视化脚本。
4. 处理结果：新增 visualize_pointcloud_samples.py，并成功生成：
   - outputs/vis/comparison_3d.png
   - outputs/vis/comparison_2d_xy.png
   - outputs/vis/summary.txt

### 1.5 开题报告阶段（多轮重写）

1. 你提出：按学校要求写开题报告五章节。
2. 处理结果：初稿生成。
3. 你提出：不要参考某份 document 正文，但参考文献只保留 document 中给出的 4 条。
4. 处理结果：整份开题报告重写并保持参考文献仅 4 条。
5. 你提出：内容扩展到 Word 超过 5 页。
6. 处理结果：扩展研究目标、问题、指标、路线、进度、风险、协作、验收等内容。
7. 你提出：多模态是导师明确主方向。
8. 处理结果：把“多模态”从可选扩展改为主线，并同步改写相关章节。

### 1.6 概念问答阶段

1. 你问：多模态是什么意思。
2. 回答结论：多模态 = 同时使用多种输入（本项目为点云 + 图像）。
3. 你问：攻击和防御是什么意思、现在该做什么。
4. 回答结论：先跑通 clean -> attack -> defense -> evaluate 闭环，再上真实数据集。
5. 你问：这些研究难度如何、能不能实现。
6. 回答结论：可实现，难度中等偏高。

### 1.7 数据集与 ART 攻击阶段

1. 你问：如何对数据集攻击、如何得到攻击后数据。
2. 回答结论：推荐离线生成对抗数据并保存新目录，不覆盖原始数据。
3. 你追问：如何使用 ART 的攻击方式对数据集攻击。
4. 回答结论：模型封装为 ART estimator，按 DataLoader 批量 generate，再保存 x_adv。

### 1.8 小组信息文件阶段

1. 你提出：按截图创建成员文件，标注组长组员。
2. 处理结果：创建小组成员文件并标注：
   - 组长：李仕晨（23009201139）
   - 组员：卢中琦（23009200116）
   - 组员：张景迪（23009200822）
   - 组员：张博远（23009200928）
   - 组员：张策涵（23009200003）

### 1.9 KITTI 下载与放置阶段

1. 你问：KITTI 那页内容要不要全下。
2. 回答结论：不用全下。
3. 推荐最小下载集（多模态检测主线）：
   - left color images
   - Velodyne point clouds
   - camera calibration matrices
   - training labels
   - object development kit
4. 你问：这五部分各做什么。
5. 回答结论：图像输入、点云输入、坐标对齐、训练监督、评测工具。
6. 你问：下载后放哪里。
7. 回答结论：放在 data/kitti 的标准目录结构（training/testing/ImageSets）。
8. 你问：KITTI 下载页面用途说明英文怎么写。
9. 处理结果：给出 2-3 句英文用途声明可直接提交。

### 1.10 迁移与会话保存阶段

1. 你问：迁移到 E 盘后聊天还在吗。
2. 回答结论：当前会话通常还在，但新会话未必继承完整上下文。
3. 你问：如何保存聊天记录。
4. 回答结论：导出聊天、Copy All、全选复制等。
5. 你同意后：已创建会话总结文件并后续扩展为完整整理版。

### 1.11 GitHub 上传阶段

1. 你问：如何上传项目到 GitHub。
2. 处理过程：
   - 初始化提交。
   - 处理远程已有提交导致的 push 拒绝。
   - rebase 过程中解决 README 冲突。
   - 成功推送 main。
3. 你问：为什么 GitHub 根目录和本地不一致。
4. 回答结论：data/outputs/work_dirs 被 .gitignore 忽略且空目录不会显示。
5. 你要求：只推大文件目录结构。
6. 处理结果：
   - 调整 .gitignore 为“忽略内容但保留骨架”。
   - 添加 .gitkeep / README 占位。
   - 成功提交并推送目录骨架。
7. 你要求：README 说明目录作用和文件格式。
8. 处理结果：重写 README（中文）并新增 README_EN（英文）。
9. 你问：能不能 push .venv。
10. 回答结论：不建议，不应上传；采用 requirements/environment.yml 重建环境。

### 1.12 E 盘克隆与运行排障阶段

1. 你提出：在 E 盘 clone 仓库。
2. 处理结果：完成克隆，目录为 E:\3d-adv-lab-clone。
3. 你提出：执行时报错。
4. 排查结果：
   - Windows 本地 .venv + Python 3.12 安装 mmcv 失败（pkg_resources 问题）。
   - 建议走 Docker 方案更稳。
5. 继续排查：
   - apt 源出现 502 / TLS 错误，导致 install_ubuntu_env.sh 在容器里安装失败。
   - 脚本曾因 CRLF/单行损坏被修复后重试。
6. 兜底方案：
   - 使用官方 CUDA 基础镜像重建。
   - 补齐运行时系统库（libgl1 等）至 Dockerfile。
   - 通过 mim 从 OpenMMlab 源直接安装 mmcv 预编译包（绕过源码编译与网络故障）。

---

## 2. 本次会话中已新增/修改的重要文件（汇总）

1. README.md（重写并加入目录与格式规范）
2. README_EN.md（英文版目录与格式规范）
3. 开题报告.md（多轮重写扩展）
4. 小组成员.md（组长/组员标注）
5. 当前会话完整整理.md（会话整理与环境问题汇总）
6. scripts/minimal_pointcloud_smoke_test.py（添加 --render 可选参数，默认无渲染）
7. scripts/visualize_pointcloud_samples.py（可视化脚本）
8. scripts/README.md（新增脚本说明）
9. docs/INSTALLATION.md（补充系统库、Windows 行尾、无头容器说明）
10. .gitignore（目录骨架策略与忽略规则调整）

---

## 3. 当前项目状态（会话结束时）

1. GitHub 仓库已建立并可推送。
2. 根目录规范文档已完整（中英文 README）。
3. 数据、输出、work_dirs 目录骨架可见，真实大文件未上传。
4. 可视化脚本可运行并已产出示例图。
5. 开题报告已对齐多模态主线。
6. E 盘克隆已完成，但环境安装受网络源不稳定影响，建议走容器镜像固化路线。

---

## 4. 你接下来可直接执行的最短路径

1. 在 E 盘项目目录运行烟雾测试（无头容器）：
   ```powershell
   docker run --rm -v "${PWD}:/workspace" -w /workspace 3d-adv-lab:cu124 bash -lc "source /workspace/.venv/bin/activate; python scripts/minimal_pointcloud_smoke_test.py --output-dir outputs/smoke_test"
   ```
2. 验证输出（outputs/smoke_test 下应有 report.json 与 .npy/.ply 文件）。
3. 若需可视化预览（需要图形环境），添加 `--render` 参数。
4. 开始 KITTI 最小集下载并按 data/kitti 结构放置。
5. 进入"真实数据 + ART 攻击 + 结果可视化"阶段。

---

## 5. 备注

- 本文件为“会话全流程整理版”，用于项目续作与迁移后的快速恢复。
- 若需要逐条逐句原始聊天全文，建议在聊天面板使用导出功能另存文本版本。
